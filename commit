
#
# use this script to add all files and commit/push the changes to git.
# 
# first, once, tell git who you are by running these commands:
#
#   git config --global user.email "you@example.com"
#   git config --global user.name "Your Name"
#
# then clone the repository to your computer, just once. Use the
# second version if you don't have public keys set up.
#
#   git clone git@github.com:<your github account>/afraid-to-commit.git
#   git clone https://github.com/<your github account>/afraid-to-commit
#
# now run this command the first time, git will prompt for the password
# once then store it indefinitively:
#
#   git config credential.helper store
#

git add *
git commit -m "changes from $(date)"
git push
